**Role:** Senior Obsidian Plugin Developer & TypeScript Expert

**Context:** I am developing a custom "Skill" (Plugin) for my Obsidian Vault located at `D:APP\MyVault`. The goal is to build a robust data retrieval system that interacts directly with the vault structure and Obsidian's Internal API.

**Task:** Create a TypeScript-based logic for a "Skill" that [ระบุความต้องการของคุณ เช่น: รวบรวมสรุปจาก Note ทั้งหมดที่มีคำว่า #project].

**Technical Execution Requirements:**

1. **Vault Interaction (The Gatekeeper):**
   - Use `this.app.vault` for all file system operations.
   - Implement `this.app.vault.getAbstractFileByPath()` to target specific folders or files within `D:\APP\MyVault`.
   - Ensure all file reads use `this.app.vault.cachedRead()` for better performance.

2. **Metadata & Structured Data Retrieval:**
   - Use `this.app.metadataCache.getFileCache()` to extract Frontmatter (YAML), tags, and headings.
   - Utilize `this.app.metadataCache.on('changed', ...)` to ensure the skill updates its data when files are modified.

3. **Data Processing Logic:**
   - Instead of standard Node.js `fs`, strictly use the Obsidian `TFile` and `TFolder` classes to navigate the folder hierarchy.
   - Filter files based on [ระบุเงื่อนไข เช่น: 'folder location' หรือ 'file properties'].

4. **Security & Validation:**
   - Add checks to ensure the skill only operates within the `D:\APP\MyVault` scope.
   - Handle cases where metadata might be missing or files are empty.

**Output:**
- Provide a clean, modular `main.ts` structure.
- Include a separate class or function specifically for 'DataFetcher' that encapsulates the `this.app.vault` and `metadataCache` logic.
- Explain how this skill will index the files efficiently without causing lag in a large vault.